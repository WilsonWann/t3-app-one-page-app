import React from "react";

type Props = {};

const ImageArea = (props: Props) => {
  return (
    <>
      {/* {arr.map((item, index) => (
        <ImageBlock key={index} customType={'static'} image={item.name} width={item.name.width} />
      ))} */}
    </>
  );
};

export default ImageArea;
